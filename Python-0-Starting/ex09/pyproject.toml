[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "A sample test package"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    {name = "frgojard", email = "frgojard@42.fr"}
]
license = {file = "LICENSE"}

[tool.setuptools.packages.find]
where = ["."]